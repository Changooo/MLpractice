MQ는 퍼블리셔, 브로커(서버), 클라이언트의 구조를 가진 통신 방법이다.
 
MQTT는 MQ를 기반으로 한 HTTP 같은 통신 프로토콜이다. 
카프카는 MQ를 기반으로 한 통신 관리 프레임워크다.

OneM2M은 IoT플랫폼의 표준으로서 MQTT, HTTP같은 통신을 추상화해줘서, api단만 신경쓰면 되는 개발환경을 제공한다.
OCF도 마찬가지 IoT플랫폼인데, MQTT를 지원하지 않고, HTTP(REST)기반의 독자 통신프로토콜을 사용한다.
